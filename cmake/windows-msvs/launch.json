{
    "configurations": [
        {
            "cwd": "${workspaceFolder}",
            "name": "Debug (lldb)",
            "program": "${command:cmake.launchTargetPath}",
            "request": "launch",
            "type": "lldb",
            "preLaunchTask": "cmake: build"
        }
    ],
    "version": "0.2.0"
}

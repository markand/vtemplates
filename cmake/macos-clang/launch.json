{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug (LLDB)",
            "program": "${workspaceFolder}/build/hello",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake: build"
        }
    ]
}

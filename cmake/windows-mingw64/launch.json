{
    "version": "0.2.0",
    "configurations": [
        {
            "environment": {
                "PATH": "@MINGW64_PATH@"
            },
            "type": "gdb",
            "program": "${command:cmake.launchTargetPath}",
            "request": "launch",
            "name": "Debug (GDB)",
            "gdb": "@MINGW64_GDB@",
            "preLaunchTask": "cmake: build"
        }
    ]
}
